"""Placeholder for a future real Consulti verification integration.

TODO: Implement only after credentials, validation rules, and approval gates are
ready.
"""

# Safety rules:
# - Do not call Consulti from this placeholder.
# - Keep mock verification as the default workflow.
# - Treat catch-all or uncertain statuses as requiring manual review.


def verify_with_consulti(*args: object, **kwargs: object) -> None:
    """Raise until the real integration is intentionally implemented."""
    raise NotImplementedError(
        "Real integration not implemented yet. Use mock/dry-run workflow until credentials and approval gates are ready."
    )
