"""Placeholder for future real Winnr inbox creation.

TODO: Implement only after credentials, domain setup, warmup policy, and approval
gates are ready.
"""

# Safety rules:
# - Do not call Winnr from this placeholder.
# - Never create paid inboxes without explicit approval.
# - Do not connect senders to live campaigns automatically.


def winnr_create_inboxes(*args: object, **kwargs: object) -> None:
    """Raise until the real integration is intentionally implemented."""
    raise NotImplementedError(
        "Real integration not implemented yet. Use mock/dry-run workflow until credentials and approval gates are ready."
    )
